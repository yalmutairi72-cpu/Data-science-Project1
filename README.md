# Capstone: A Backtested Forecasting Report — Economic Activity Index

**Training programme:** Time Series Forecasting for AI Systems (السلاسل الزمنية والتنبؤ) — SDAIA Academy
**Cohort:** الفصل الأول 2026 (First Semester 2026)
**Team:** Yousef Al-Mutairi · Abdulwahab Al-Nassar · Firas Al-Nasser · Anas Al-Zahrani · Khalid Al-Mutairi

Course site: https://mohammadyusif.github.io/time-series-forecasting-ai-systems
Course repository: https://github.com/MohammadYusif/time-series-forecasting-ai-systems

## Project idea

This is the capstone project for the *Time Series Forecasting for AI Systems*
course: one self-contained Colab notebook that takes a single time series from
raw data to a validated, uncertainty-aware forecast, exactly the shape every lab
in the course builds toward.

## Which dataset, and why

I chose **`data/economic_indicator.csv`** — a monthly economic activity index,
108 points from January 2017 to December 2025 — over the other three course
datasets (`retail_demand.csv`, `workforce_demand.csv`, `intermittent_demand.csv`).
It is the shortest and lowest-frequency series in the course, which is exactly the
point: with only ~8 full seasonal cycles of monthly data, it is the one dataset
that forces the "does history length change which tool is even appropriate"
question (`day3/06_model_comparison.qmd`) to actually matter for the result,
rather than being a side note next to a strong accuracy number.

As with every dataset in this course, the series is **synthetic**, generated by a
seeded script (`data/generate_series.py`) in the original course repository.
Nothing in this notebook describes any real country's actual economic indicator.

## What's in the notebook

`capstone_economic_indicator.ipynb` runs top to bottom with real, captured output
and covers, in order:

1. **Decomposition & diagnostics** — STL decomposition, ACF/PACF, the ADF
   stationarity test, and what differencing the series needs.
2. **A classical model** — SARIMA, with the order chosen by an AIC grid search
   and a stated rationale, plus a Ljung-Box residual diagnostic.
3. **An ML/GBM model** — LightGBM with lag/rolling/calendar features, forecasting
   recursively, with an explicit discussion of leakage risk in that recursion.
4. **A 4-fold walk-forward backtest** — using the course's own
   `common/backtest.py` harness (`expanding_window_splits` + `run_backtest`),
   with the expanding-vs-rolling choice justified for this series.
5. **Accuracy metrics** — MAE, RMSE, and MASE (justified over WAPE/MAPE for this
   series' shape) via the course's `common/metrics.py`.
6. **Probabilistic forecasts** — a split-conformal interval for LightGBM and
   SARIMA's native interval, both scored with `coverage()` **and**
   `interval_width()` together, not coverage alone.
7. **A written model-comparison and recommendation** — reasoning from history
   length, interpretability, interval support, and compute budget, not accuracy
   alone.

## How to open and run it

**Recommended — Google Colab, no local setup:**

[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/YOUR-GITHUB-USERNAME/time-series-capstone-economic-indicator/blob/main/capstone_economic_indicator.ipynb)

> Replace `YOUR-GITHUB-USERNAME/time-series-capstone-economic-indicator` above
> (and inside the notebook's own badge cell) with this repository's real path
> once it's pushed to GitHub, so the badge opens *this* repo's notebook.

Click the badge, then run the first code cell and every cell after it in order.
The first cell installs every package it needs (`pandas`, `numpy`, `matplotlib`,
`statsmodels`, `lightgbm`, `scikit-learn`) and downloads the shared
`common/metrics.py`, `common/backtest.py`, and the dataset from the course's
GitHub repository if they aren't already present locally — no manual install,
no API key, no account required, per the course's own `setup.qmd`.

**Locally, if you'd rather not use Colab:**

```bash
git clone https://github.com/YOUR-GITHUB-USERNAME/time-series-capstone-economic-indicator.git
cd time-series-capstone-economic-indicator
pip install pandas numpy matplotlib statsmodels lightgbm scikit-learn jupyter
jupyter notebook capstone_economic_indicator.ipynb
```

## Repository structure

```
.
├── capstone_economic_indicator.ipynb   # the capstone notebook (run this)
├── README.md                           # this file
├── docs/
│   └── TECHNICAL.md                    # technical documentation of the notebook
├── common/
│   ├── backtest.py                     # course-provided walk-forward harness
│   └── metrics.py                      # course-provided accuracy/interval metrics
├── data/
│   └── economic_indicator.csv          # the chosen dataset (synthetic)
└── .gitignore
```

`common/` and `data/` are vendored copies of the shared course files this
notebook depends on (see attribution below) — the notebook also works with
neither present, since its first cell fetches both from the course's GitHub
repository automatically.

## Results at a glance

| Model | MAE | RMSE | MASE | WAPE (%) |
|---|---|---|---|---|
| Seasonal-naive baseline | 11.19 | 12.65 | 1.35 | 7.20 |
| **SARIMA(0,1,2)(0,0,1,12)** | **3.07** | **4.12** | **0.37** | **1.97** |
| LightGBM (recursive) | 4.97 | 5.83 | 0.60 | 3.19 |

Both real models comfortably beat the naive baseline; SARIMA wins across the
full 4-fold backtest, not just a single lucky holdout window — see the
notebook's Section 7 for the full reasoning behind recommending it for
deployment on this series.

## Attribution

`common/backtest.py` and `common/metrics.py`, the course's four datasets and
`data/generate_series.py`, and the overall notebook structure are from the
*Time Series Forecasting for AI Systems* course by Mohammad Yusif for SDAIA
Academy: https://github.com/MohammadYusif/time-series-forecasting-ai-systems.
The analysis, model choices, diagnostics, and written interpretation in
`capstone_economic_indicator.ipynb` are my own work for this capstone.

## SDAIA Academy

This project was completed as part of an SDAIA Academy training programme.
SDAIA Academy's GitHub: https://github.com/SDAIAAcademy
